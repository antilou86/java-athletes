package com.lambdaschool.solution;

//creates generic interface so we can override default processor functionality. also give main.java access to .displayAthlete method.
public interface Processor {
    void displayAthlete();
}